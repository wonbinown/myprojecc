<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.MemberMapper">

  <select id="findByUsername" parameterType="string" resultType="model.Member">
    SELECT MEMBER_SEQ, name, username, password, email
    FROM member
    WHERE username = #{username}
  </select>

  <insert id="insertMember" parameterType="model.Member">
    INSERT INTO member (MEMBER_SEQ, name, username, password, email)
    VALUES (member_seq.NEXTVAL, #{name}, #{username}, #{password}, #{email})
  </insert>

</mapper>
